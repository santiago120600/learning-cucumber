# Learning BDD with cucumber

### Run project from cmd
`gradle cucumber`

Don't run test with @ignore tag

`gradle cucumber --tags "not @ignore"`

Run only test with @Test tag

`gradle cucumber --tags "@Test"`